<aside data-ng-cloak>
    <h4>Template Description</h4>
    <span class="help-block" data-ng-hide="template.title">No template selected!</span>
    <div data-ng-show="template.title">
        <div class="panel panel-default">
            <div class="panel-body">
                <h4>{{ template.title }}</h4>
                <p>{{ template.description }}</p>
            </div>
        </div>
    </div>
</aside>
